class car:
	def __init__ (Self, b_name, model, types):
		Self.b_name = b_name
		Self.model = model
		Self.types = types

car1 = car("Red Bull Honda", "RB18", "F1")

print(car1.b_name, car1.model, car1.types)